main(){

    int i=0;
    int j=10;
    int k=20;
    int l=30;

    spawn{while(i<10) printf("%d",i); }
    spawn{while(j<20) printf("%d",j); }
    spawn{while(k<30) printf("%d",k); }
    spawn{while(l<40) printf("%d",l); }
    
    barrier;
    print("Done \n");
}
