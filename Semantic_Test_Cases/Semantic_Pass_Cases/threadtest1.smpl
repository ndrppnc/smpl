int limit=0;
void main()
{
    int i = 0;
    int sum = 0;
    int n;
    int t;
    printf("Enter the number of threads t: ");
        lock {
            sum = sum + i;
        }
   
    printf("The sum of the first %d numbers is %d.\n",
           limit,sum);
}
